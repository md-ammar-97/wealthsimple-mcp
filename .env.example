# LLM providers
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...

# API key for the live google-mcp-server on Cloud Run
# Must match SERVER_API_KEY stored in Secret Manager on the server side
# Clients send this as the X-Api-Key: <value> HTTP header
MCP_API_KEY=
